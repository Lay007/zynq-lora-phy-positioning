# Приёмка M2: измерения Simulink

[English version](../simulink-m2-acceptance.md)

Измеренные результаты исполняемой модели Simulink. Каждое число получено
зафиксированным скриптом и подтверждено зафиксированной таблицей; оценок «на
глаз» здесь нет. Контракт интерфейсов, инструментарий и формат векторов
приведены в [основаниях M2](simulink-m2-interfaces.md).

M2 **не завершён**. Документ фиксирует измеренное и явно называет то, что ещё
блокирует приёмку.

## Воспроизведение

```matlab
cd model/simulink
results = run_simulink_regression;   % toolchain, double, joint, reset, acquisition, blind
results = run_simulink_regression(Suites=["fixed" "real"]);   % длинные кампании
report = run_hdl_generation;         % Verilog и счётчики ресурсов
```

Команда возбуждает ошибку при любом несовпадении, поэтому

```bash
matlab -batch "cd model/simulink; run_simulink_regression"
```

возвращает ненулевой код завершения, когда Simulink и MATLAB расходятся. Это
проверено принудительным допуском `1e-20`: прогон завершается с кодом 1.

Удаление `model/simulink/generated/` перед прогоном безопасно и является частью
проверки: каждая модель заново собирается своим скриптом генерации.

## Поэтапная эквивалентность в double

Стимулом служит зафиксированный golden-этап `input`, поэтому проверяется
только тракт Simulink. Все 15 acceptance-случаев пройдены.

| Результат | Значение |
|---|---|
| Случаев | 15 / 15 |
| Решения по символам совпадают с MATLAB | 15 / 15 |
| Худшая относительная RMS-ошибка по всем этапам | `8,334e-16` |
| Худшая абсолютная ошибка confidence | `1,887e-15` |
| ROM опоры против golden `conjReferenceSpectrum` | `0` (побитово) |
| Применённый допуск приёмки | относительная RMS ≤ `1e-12` |

По точкам сравнения, худшее по всем 15 случаям:

| Этап | Сравнено элементов | Макс. абсолютная ошибка | Худшая относительная RMS |
|---|---:|---:|---:|
| `fftM` | 16384 | `1,400e-13` | `4,051e-16` |
| `product` | 16384 | `1,150e-11` | `4,110e-16` |
| `partition` | 7904 | `1,140e-11` | `4,110e-16` |
| `fftN` | 7904 | `2,572e-12` | `8,334e-16` |
| `magnitudeSquared` | 7904 | `5,821e-10` | `5,786e-16` |

Абсолютная ошибка растёт вместе с масштабом этапа и сама по себе ничего не
значит: значения `fftM` достигают `M` амплитуд входа, а `magnitudeSquared` —
это квадрат. Относительная RMS везде остаётся на уровне `1e-16`, то есть это
округление double, а не алгоритмическое расхождение.

Исходные таблицы:

- [`simulink-m2-stage-comparison.csv`](../data/simulink-m2-stage-comparison.csv) —
  строка на каждый случай и этап.
- [`simulink-m2-case-summary.csv`](../data/simulink-m2-case-summary.csv) —
  строка на случай с решениями, confidence, latency и throughput.

## Latency и throughput

Latency отсчитывается от **последнего входного отсчёта окна символа** — самого
раннего момента, когда решение по этому символу вообще возможно. Все значения
в тактах отсчётов; DUT обрабатывает один отсчёт за такт.

| SF | L | N | M | Latency FFT_M | Latency символа | Интервал символов |
|---:|---:|---:|---:|---:|---:|---:|
| 5 | 4 | 32 | 128 | 180 | 419 | 128 |
| 5 | 8 | 32 | 256 | 313 | 680 | 256 |
| 7 | 1 | 128 | 128 | 180 | 614 | 128 |
| 7 | 2 | 128 | 256 | 313 | 875 | 256 |
| 7 | 4 | 128 | 512 | 567 | 1385 | 512 |
| 7 | 8 | 128 | 1024 | 1084 | 2414 | 1024 |
| 9 | 2 | 512 | 1024 | 1084 | 3185 | 1024 |
| 10 | 1 | 1024 | 1024 | 1084 | 4214 | 1024 |
| 12 | 1 | 4096 | 4096 | 4143 | 16476 | 4096 |

Пропускная способность — **один символ на `M` отсчётов без пропусков**.
Измеренный интервал символов ровно равен `M` во всех многосимвольных случаях,
то есть конвейер держит полную входную скорость и никогда не останавливается.
Именно это измерение стоит за решением отказаться от backpressure.

Latency символа зависит не только от `M`, но и от `N`: при `M = 1024` она равна
2414 отсчётов для `N = 128` и 4214 для `N = 1024`, поскольку второй FFT и peak
tracker масштабируются с `N`.

### Чем эти числа не являются

Это latency в тактах отсчётов модели Simulink. Это **не** аппаратная задержка:
тактовой частоты, `Fmax` и оценок ресурсов пока не существует. Синтез не
выполнялся.

## Конфигурация fixed point

### Что выбирается, а что вытекает

Целые биты на каждой выбранной границе берутся из измеренного диапазона по
зафиксированным golden-векторам (`lora_sim.stage_ranges`) плюс один guard-бит,
поэтому варьируемая разрядность меняет только дробную часть. Политика
удерживается постоянной, чтобы sweep менял ровно одну переменную:

| Свойство | Значение | Почему |
|---|---|---|
| Округление | `Floor` | усечение, самый дешёвый вариант в HDL |
| Переполнение | `Saturate` | перенос сдвинул бы `argmax` |
| Guard-биты | 1 | одно двоичное удвоение измеренного диапазона |

Выбираемые границы: входной IQ, ROM опоры, комплексное умножение, аккумулятор
секций, второй FFT после масштаба `1/M`, magnitude², сумма спектра и confidence.

Разрядности двух FFT **не выбираются**. `dsphdl.FFT` работает без нормализации,
поэтому его выходное слово равно входному плюс `log2(FFTLength)` при неизменной
дробной части. Они приведены как производные величины.

### Почему сохранения решений недостаточно

Очевидное правило приёмки — «символьные решения не должны измениться» —
выполняется при разрядностях, численно бесполезных, и sweep говорит об этом
прямо. При **8 битах** каждый acceptance-случай сохраняет точный символ MATLAB,
хотя относительная RMS-ошибка выхода коррелятора достигает **118 %**.

Причина видна в столбце запаса решения: относительный разрыв между победившим
бином и вторым по величине лежит в диапазоне 0,16…1,00 по всему
acceptance-набору и в основном выше 0,8. Сломать `argmax` при таком запасе
требует огромного шума квантования.

Но `argmax` — не единственный потребитель. `decode_packet_soft` использует
пер-биновые магнитуды как информацию о надёжности, и демодулятор с ошибкой в
десятки процентов там бесполезен. Поэтому выбор требует одновременно:

1. каждый acceptance-случай принимает то же решение, что и MATLAB;
2. худшая относительная RMS-ошибка по всем этапам не превышает
   `MaxRelativeRms`, по умолчанию `1e-2`.

Граница 1 % — инженерное решение, а не выведенный предел, и она заявлена именно
так. Обоснование: в acceptance-точке `−10 дБ` SNR значения magnitude² уже
изменяются на величину порядка 100 % от одного шума, поэтому удержание
квантования на уровне 1 % оставляет его примерно на 20 дБ ниже физической
изменчивости, с которой рассчитан работать мягкий декодер.

### Результаты sweep

15 случаев на каждую разрядность, 90 fixed-point симуляций, 3161 с суммарно.

| Разрядность | Случаев | Совпало решений | Худшая отн. RMS | Худшая ошибка confidence | Наименьший запас решения |
|---:|---:|---:|---:|---:|---:|
| 8 | 15 | 15 | `1,180e+00` | `3,628e-01` | 0,160 |
| 10 | 15 | 15 | `2,922e-01` | `1,748e-01` | 0,227 |
| 12 | 15 | 15 | `7,334e-02` | `8,604e-02` | 0,232 |
| 14 | 15 | 15 | `1,805e-02` | `2,781e-02` | 0,236 |
| **16** | 15 | 15 | `4,561e-03` | `7,459e-03` | 0,237 |
| 18 | 15 | 15 | `1,142e-03` | `2,049e-03` | 0,237 |

Худшая относительная RMS по конфигурациям:

| SF | L | M | 8 бит | 10 бит | 12 бит | 14 бит | 16 бит | 18 бит |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 5 | 4 | 128 | `1,902e-01` | `4,631e-02` | `1,205e-02` | `3,198e-03` | `8,980e-04` | `2,266e-04` |
| 5 | 8 | 256 | `1,242e-01` | `5,669e-02` | `1,434e-02` | `3,396e-03` | `8,579e-04` | `2,330e-04` |
| 7 | 1 | 128 | `1,993e-01` | `5,043e-02` | `1,242e-02` | `3,161e-03` | `7,856e-04` | `1,960e-04` |
| 7 | 2 | 256 | `1,675e-01` | `4,448e-02` | `1,063e-02` | `2,787e-03` | `6,844e-04` | `1,707e-04` |
| 7 | 4 | 512 | `1,751e-01` | `5,222e-02` | `1,227e-02` | `3,097e-03` | `7,987e-04` | `2,034e-04` |
| 7 | 8 | 1024 | `3,389e-01` | `1,133e-01` | `3,840e-02` | `9,696e-03` | `2,410e-03` | `5,635e-04` |
| 9 | 2 | 1024 | `3,943e-01` | `8,954e-02` | `2,271e-02` | `5,638e-03` | `1,419e-03` | `3,667e-04` |
| 10 | 1 | 1024 | `5,846e-01` | `1,492e-01` | `3,644e-02` | `8,877e-03` | `2,292e-03` | `5,846e-04` |
| 12 | 1 | 4096 | `1,180e+00` | `2,922e-01` | `7,334e-02` | `1,805e-02` | `4,561e-03` | `1,142e-03` |

Каждые два добавленных бита делят ошибку примерно на четыре, как и ожидается
для чисто дробного уточнения.

**Выбрано: 16 бит** для всего acceptance-набора. Это наименьшая разрядность из
перебранных, удовлетворяющая обоим критериям; 14 бит не проходят только из-за
`SF12, L=1` со значением `1,805e-02`.

**Для первой аппаратной цели достаточно 14 бит.** При `SF7, L=8` — BW 125 кГц и
`Fs = 1 МГц` — ошибка при 14 битах равна `9,696e-03`, внутри границы 1 %.
Большая разрядность продиктована самым широким FFT в acceptance-наборе, а не
той конфигурацией, которую плата запустит первой.

Выбор выводится из зафиксированной таблицы; оба критерия закодированы в
`run_fixed_point_sweep` параметром `MaxRelativeRms`, поэтому повторный запуск
воспроизводит выбор, а не требует ручного вывода.

### Выбранные типы при 16 битах

| SF | L | input | reference | product | accumulator | fftN | magnitude² | FFT_M out | FFT_N out |
|---:|---:|---|---|---|---|---|---|---:|---:|
| 5 | 4 | `sfix16_En11` | `sfix16_En9` | `sfix16_En3` | `sfix16_En3` | `sfix16_En6` | `ufix16_En0` | 23 | 21 |
| 5 | 8 | `sfix16_En14` | `sfix16_En8` | `sfix16_En2` | `sfix16_En2` | `sfix16_En6` | `ufix16_En-1` | 24 | 21 |
| 7 | 1 | `sfix16_En14` | `sfix16_En10` | `sfix16_En6` | `sfix16_En6` | `sfix16_En7` | `ufix16_En1` | 23 | 23 |
| 7 | 2 | `sfix16_En14` | `sfix16_En9` | `sfix16_En4` | `sfix16_En4` | `sfix16_En6` | `ufix16_En-1` | 24 | 23 |
| 7 | 4 | `sfix16_En14` | `sfix16_En8` | `sfix16_En2` | `sfix16_En2` | `sfix16_En5` | `ufix16_En-3` | 25 | 23 |
| **7** | **8** | `sfix16_En10` | `sfix16_En7` | `sfix16_En-1` | `sfix16_En-1` | `sfix16_En4` | `ufix16_En-5` | 26 | 23 |
| 9 | 2 | `sfix16_En12` | `sfix16_En8` | `sfix16_En1` | `sfix16_En1` | `sfix16_En4` | `ufix16_En-5` | 26 | 25 |
| 10 | 1 | `sfix16_En14` | `sfix16_En8` | `sfix16_En3` | `sfix16_En3` | `sfix16_En4` | `ufix16_En-5` | 26 | 26 |
| 12 | 1 | `sfix16_En14` | `sfix16_En7` | `sfix16_En1` | `sfix16_En1` | `sfix16_En2` | `ufix16_En-9` | 28 | 28 |

Отрицательные дробные разряды — не ошибка: `magnitudeSquared` при `SF7, L=8`
достигает `1,05e6`, поэтому тип представляет собой 16-битное беззнаковое
значение с масштабом `2^5`. Столбцы выходов FFT производные, а не выбранные.

Исходные таблицы:

- [`simulink-m2-fixed-point-sweep.csv`](../data/simulink-m2-fixed-point-sweep.csv)
- [`simulink-m2-fixed-point-cases.csv`](../data/simulink-m2-fixed-point-cases.csv)
- [`simulink-m2-fixed-point-stages.csv`](../data/simulink-m2-fixed-point-stages.csv)

### Range analysis ограничен по области, и это ограничение

Целые биты следуют измеренным максимумам acceptance-корпуса. Другой входной
режим — front end с другим запасом, другая цель AGC, помеха — требует
повторного range analysis. Переносимым результатом является разрядность;
дробные части — нет.

## Совместная оценка timing/CFO

Вторая подсистема разделяет целочиповый timing и смещение несущей по знаковым
dechirp-бинам одного upchirp преамбулы и одного downchirp SFD. Поскольку
half-sum и half-difference — точные кратные `1/2`, вся оценка представляет собой
целочисленную арифметику в единицах половины бина, и DUT сравнивается с MATLAB
на **точное равенство**, а не в пределах допуска.

| SF | L | N | Пар бинов | Покрытие | Расхождений | Отклонённых пар |
|---:|---:|---:|---:|---|---:|---:|
| 5 | 8 | 32 | 1 024 | полный перебор | 0 | 0 |
| 7 | 8 | 128 | 16 384 | полный перебор | 0 | 0 |
| 7 | 1 | 128 | 16 384 | полный перебор | 0 | 0 |
| 9 | 2 | 512 | 262 144 | полный перебор | 0 | 0 |

295 936 пар бинов, каждая из которых перебрана: это доказательство на всей
области определения, а не выборка. Совпадают все пять выходов —
`correctionSamples`, `cfoHalfBins`, `timingHalfChips`, `estimateValid`,
`timingRejected`.

Исходная таблица:
[`simulink-m2-joint-sync.csv`](../data/simulink-m2-joint-sync.csv).

Ноль в последнем столбце сам по себе является результатом. Защитная проверка,
обнуляющая неправдоподобную временную поправку, **недостижима** для бинов из
допустимого диапазона: знаковые бины ограничивают `|timingChips|` величиной
`(N-1)/2`, поэтому поправка не может превысить `(N-1)·L/2`, а это никогда не
достигает порога `M/2 + L`. Ветка сохранена для вызывающих, которые могут
искать в более широком диапазоне, и теперь и MATLAB-тест, и этот прогон
Simulink фиксируют её как неактивную, а не оставляют молча мёртвой.

Оценщик потребляет бины, а не отсчёты. То, что эти бины производит, теперь в
модели есть: слепое обнаружение начала пакета описано ниже и выдаёт грубое
смещение в целых чипах, которое оценщик уточняет до половин чипа.

## Реальные окна символов SX1262

Зафиксированные записи SX1262 → ZynqSDR декодируются авторитетным
MATLAB-приёмником, скорректированные окна символов, которые коррелятор реально
потребил, извлекаются, нормируются по RMS на пакет и проигрываются через
fixed-point DUT при 16 битах.

| SF | L | M | Пакетов | Символов | fixed == float | nominal == receiver | худшая отн. RMS |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 5 | 8 | 256 | 30 | 2340 | 2340 | 2340 | `8,409e-04` |
| 6 | 8 | 512 | 30 | 2040 | 2040 | 2040 | `8,772e-04` |
| 7 | 8 | 1024 | 70 | 4060 | 4060 | 4060 | `1,399e-03` |
| **всего** | | | **130** | **8440** | **8440** | **8440** | `1,399e-03` |

Все 130 зафиксированных пакетов, все 8440 потреблённых символов: 16-битный
fixed-point DUT принимает ровно то же решение, что и floating-point коррелятор,
и оба совпадают с тем, что решил MATLAB-приёмник пакетов. Худшая ошибка
confidence — `6,704e-03`. Ни в одном пакете нет ни одного расходящегося символа.

Столбец `nominal == receiver` заслуживает отдельного предложения. DUT использует
номинальный опорный chirp; пакетный приёмник дополнительно оценивает
выровненную по фазе опору по преамбуле. На этих записях адаптивная опора не
меняет **ни одного** решения по символу, то есть функция, которой в DUT нет, для
данного корпуса не является определяющей на уровне символов. Это измерение на
130 пакетах, а не общее утверждение.

Range analysis для этого прогона собран по самим реальным окнам. Выбранные типы:

| SF | L | input | reference | product | accumulator | fftN | magnitude² |
|---:|---:|---|---|---|---|---|---|
| 5 | 8 | `sfix16_En13` | `sfix16_En8` | `sfix16_En2` | `sfix16_En2` | `sfix16_En6` | `ufix16_En-1` |
| 6 | 8 | `sfix16_En13` | `sfix16_En7` | `sfix16_En1` | `sfix16_En1` | `sfix16_En5` | `ufix16_En-3` |
| 7 | 8 | `sfix16_En13` | `sfix16_En7` | `sfix16_En0` | `sfix16_En0` | `sfix16_En3` | `ufix16_En-6` |

Исходные таблицы:

- [`simulink-m2-real-iq-groups.csv`](../data/simulink-m2-real-iq-groups.csv)
- [`simulink-m2-real-iq-packets.csv`](../data/simulink-m2-real-iq-packets.csv)

### Что означает число 8440 и какой дефект нашёл этот прогон

8440 — это число символов, которые декодер **потребил**: 78 на пакет при SF5,
68 при SF6, 58 при SF7. Это не число окон, которые выдал приёмник. При поиске
тайминга приёмник демодулирует все окна до энергетической границы пакета, а она
с запасом: пакет SF7 даёт около 133 окон, из которых сигнал несут 58, а
остальные — шумовая полка после окончания передачи.

Первая версия этой регрессии проигрывала их все и показала 45 % совпадений.
Ошибки этапов с этим числом не сходились — у SF5 относительная RMS была 0,6 %
при 46 % расходящихся решений, что невозможно, — и именно это противоречие
вскрыло причину. В шумовых окнах fixed-point `magnitudeSquared` оказался
**тождественно нулевым по всем бинам**: их пик около `0,13` против диапазона
корпуса `2,4e6`, поэтому весь спектр ушёл под LSB, а `argmax` вернул бин 0.
Double-версия DUT на том же пакете дала 133/133 — это и доказало, что структура
верна, а неверен стимул.

Отсюда стоит забрать две вещи. Оценивать `argmax` по шуму бессмысленно и в
floating point — просто там он молча совпадает сам с собой. И fixed point
превратил тихую бессмыслицу в громкий ноль, а это более полезный режим отказа.

## Совместимость с HDL Coder

Критерий M2 — не только совпадение Simulink с MATLAB, но и прохождение проверок
HDL Coder. `run_hdl_compatibility_check` запускает `checkhdl` на каждом
именованном DUT с `TargetLanguage`, равным Verilog.

| Цель | DUT | Ошибок | Предупреждений |
|---|---|---:|---:|
| `fft-correlator-double` | `lora_fft_correlator_hdl/DUT` | 7 | 2 |
| **`fft-correlator-fixed`** | `lora_fft_correlator_hdl/DUT` | **0** | 2 |
| **`joint-timing-cfo`** | `lora_joint_sync_hdl/DUT` | **0** | 2 |

Проверяемые DUT собираются с `IncludeVerificationTaps=false`, поэтому на
границе HDL остаются только production-сигналы, а stage-тапы, нужные регрессии,
в железо не попадают.

| Цель | Ошибок | Предупреждений |
|---|---:|---:|
| `fft-correlator-double` | 7 | 2 |
| `fft-correlator-fixed` | 0 | 2 |
| `acquisition` | 0 | 2 |
| `joint-timing-cfo` | 0 | 2 |

Все три DUT, которым предстоит стать железом, проходят без ошибок. Семь ошибок
double-модели — это ожидаемый и правильный результат: `Double and Single data
types are not supported for HDL code generation`. Double-модель является
эталоном симуляции, а не целью HDL, и проверяется только для того, чтобы
разница была зафиксирована.

Два предупреждения одинаковы у всех трёх и носят информационный характер:
Verilog разворачивает векторные сигналы независимо от `ScalarizePorts`, а
разделение HDL между идентичными atomic subsystem требует
`Default parameter behavior` = `Inlined`. Одно сообщение на fixed-point DUT
стоит перенести в M3: делитель confidence использует архитектуру `ShiftAdd` и
потому добавляет задержку, которой нет в текущей таблице latency по тактам
отсчётов.

Исходные таблицы:
[`simulink-m2-hdl-compatibility.csv`](../data/simulink-m2-hdl-compatibility.csv),
[`simulink-m2-hdl-messages.csv`](../data/simulink-m2-hdl-messages.csv).

### Гейтом является не checkhdl, а генерация

Более ранняя версия этого документа считала чистый `checkhdl` доказательством
готовности DUT к HDL. Это неверно, и подсистема acquisition — контрпример:
`checkhdl` показал **0 ошибок**, после чего `makehdl` отказался её генерировать.

```text
Found unsupported division expression for HDL code generation;
Signed input data type is not supported for division with Floor RoundMode
```

Причиной был `mod(bin - target + N/2, N)` в вычислении круговой дистанции.
`mod` от знакового значения — это деление, а знаковое деление с округлением
Floor HDL Coder не генерирует. Оба бина лежат в `[0, N)`, поэтому разность
находится в `(-N, N)`, и перенос чинится одним сравнением, без деления:

```text
delta = bin - target;
if delta >=  N/2, delta = delta - N; end
if delta <  -N/2, delta = delta + N; end
```

Joint-оценщик изначально обходился без `mod` и генерировался чисто — поэтому
расхождение проявилось только здесь. Правило, которое из этого следует:
**подсистема считается готовой к HDL, когда `makehdl` выдаёт Verilog, а не
когда молчит `checkhdl`.** Поэтому `run_hdl_generation` входит в путь приёмки,
а не является необязательным дополнением.

### Два дефекта, найденных этой проверкой

`checkhdl` оправдал себя тем, что отверг настоящую ошибку границы, а не
стилистическую придирку:

```text
Illegal conversion to or from floating-point in .../DUT/CastInput
```

Внутри DUT стоял блок `Data Type Conversion`, превращавший `double` на входном
порту в fixed-point тип отсчёта. В симуляции это удобно, в железе бессмысленно:
АЦП уже отдаёт fixed-point поток. Теперь квантователь стоит в обвесе, на один
блок раньше; числа те же, а на границе HDL плавающей точки больше нет.

Вторым был делитель: HDL Coder принимает на делении только округление `Zero`
или `Simplest` и требует насыщения. Confidence — отношение неотрицательных
величин, поэтому округление к нулю совпадает с `Floor`, применяемым везде
остальном, и смена ничего не стоит.

Две механические ловушки записаны, потому что стоили времени. `checkhdl`
компилирует модель, поэтому источникам `From Workspace` в обвесе нужен стимул в
base workspace, иначе модель не инициализируется ещё до единой проверки. И
настройки HDL Coder ставятся через `hdlset_param`; `TargetLang` у `set_param` —
это селектор C/C++ из Simulink Coder, и он отвергает `"Verilog"`.

Третья ловушка давала не ошибку, а неверный результат. В структуре сообщений
`checkhdl` поле `type` — это `model` или `block`, а серьёзность лежит в `level`.
Подсчёт ошибок по `type` выдавал «0 errors, 0 warnings» при живых находках.
Первая опубликованная версия этой таблицы была неверна именно поэтому.

### Что это не доказывает

`checkhdl` — проверка совместимости, а не компилятор. `makehdl` не запускался,
Verilog не существует, cosimulation не выполнялась, синтеза не было. Чисел по
ресурсам, `Fmax` и мощности нет, и они не заявляются.

## Поведение reset и метаданные времени

`resetIn` очищает три счётчика и оба потоковых FFT. Больше ничего в сбросе не
нуждается: обратная связь аккумулятора закрыта на первых `N` бинах символа, а
peak tracker переинициализируется на бине 0, поэтому ни один из них не может
прочитать состояние до сброса, как только счётчики стартуют заново. Это
архитектурное свойство, а не предположение, поэтому оно проверяется, а не
декларируется.

`run_reset_regression` подаёт один сигнал, выставляет reset, подаёт второй
сигнал в той же симуляции и требует, чтобы вторая половина точно совпала с
отдельным прогоном этого сигнала. Дополнительно требуется, чтобы счётчик
отсчётов начинался с нуля.

| SF | L | M | Символов до reset | Символов после reset | Совпало с отдельным прогоном | Метки перезапущены |
|---:|---:|---:|---:|---:|---|---|
| 5 | 2 | 64 | 3 | 2 | да | да |
| 7 | 8 | 1024 | 3 | 2 | да | да |

Одна деталь стоила цикла отладки и заслуживает записи: если выставить reset в
том же такте, что и первый валидный отсчёт, потоковый FFT отбрасывает этот
кадр. Импульс reset должен приходиться на холостой такт до начала данных —
именно так теперь и делает обвес.

`symbolSampleCount` несёт индекс первого отсчёта каждого символа, защёлкнутый на
`symbolBoundary` и поставленный в очередь до выхода решения. Поэтапная
регрессия проверяет его на каждом acceptance-случае: метки обязаны быть ровно
`0, M, 2M, …`, и максимальная ошибка равна `0` во всех 15.

`fractionalToaSamples` **не реализован**, и это осознанно. MATLAB-функция
`estimate_fractional_toa` подгоняет параболу по трём точкам мощности корреляции
на сетке отсчётов, а для этого нужен matched filter на частоте отсчётов,
относящийся к acquisition. Спектр коррелятора разрешает лаги с шагом `L`
отсчётов, поэтому та же парабола дала бы дробный *бин*, а не субдискретный ToA.
Это разные величины, поэтому DUT отдаёт грубый счёт, а дробное уточнение
остаётся в программной части.

Исходная таблица: [`simulink-m2-reset.csv`](../data/simulink-m2-reset.csv).

## Acquisition: приём преамбулы и sync word

Третья подсистема потребляет поток символов коррелятора и решает, похоже ли
увиденное на последовательность захвата LoRa: `PreambleSymbols` upchirp около
бина 0, затем два символа sync word около бинов, которые кодирует sync word,
каждый в пределах одного бина по круговой метрике.

Как и joint-оценщик, это целочисленная арифметика по бинам, поэтому DUT
сравнивается с `lora_phy.validate_acquisition_bins` на **точное равенство**.

| SF | Символов преамбулы | N | Последовательностей | Принято MATLAB | Расхождений sync | Расхождений преамбулы | Расхождений флага отказа |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 7 | 8 | 128 | 205 | 2 | 0 | 0 | 0 |
| 5 | 8 | 32 | 205 | 2 | 0 | 0 | 0 |
| 9 | 6 | 512 | 205 | 2 | 0 | 0 | 0 |

615 последовательностей, ноль расхождений по всем трём выходам. Каждая
конфигурация смешивает значимые случаи — точный захват, дрейф на один бин
включая переход через ноль, дрейф вне допуска по преамбуле, дрейф вне допуска
по sync word, полностью неверный sync word — с 200 псевдослучайными
последовательностями, которые практически всегда отвергаются. Принято лишь 2 из
205, и в этом смысл: детектор, принимающий всё подряд, прошёл бы тест, которому
подавали только корректный вход.

Исходная таблица:
[`simulink-m2-acquisition.csv`](../data/simulink-m2-acquisition.csv).

Одна константа закреплена отдельным тестом, потому что ошибка в ней —
классическая и дорогая: LoRa кодирует каждый ниббл sync word как индекс символа,
умноженный на **8**; масштаб фиксирован и не равен `2^(SF-4)`. MATLAB-тест
проверяет отображение полным перебором всех 256 sync word и по SF7…SF12.

### Чем эта подсистема не является

Она проверяет **выровненного по символам кандидата** по абсолютным целям: бин 0
для преамбулы и `8 x ниббл` для sync word. Это верная проверка после того, как
timing и CFO уже скорректированы, и неверная — до этого. О том, как пакет
находится изначально, — следующий раздел.

## Слепое обнаружение начала пакета

Эта подсистема находит пакеты, а не подтверждает их, и обошлась она гораздо
дешевле, чем предсказывал этот же документ раньше. Прогноз процитирован и
исправлен в разделе о ресурсах ниже.

### Почему скользящая корреляция не нужна

Ожидаемая стоимость исходила из предположения о поиске по смещениям отсчётов.
Такого поиска нет — из-за свойства самого сигнала.

Преамбула — это буквальное повторение одного upchirp, поэтому передаваемый блок
периодичен с периодом `samplesPerSymbol`. Значит, любое окно такой длины является
циклическим сдвигом опорного чирпа, а сдвиг всё равно даёт чистый тон после
dechirp. Возьмём окно, начинающееся через `d` чипов после границы символа: на
локальном чипе `c` оно несёт чип сигнала `mod(c + d, N)` с нормированной частотой
`mod(c + d, N)/N - 1/2`, а сопряжённая опора даёт `-(c/N - 1/2)`. Произведение
стоит на `d/N` при любом `c`. Отсюда

```text
бин = d, число чипов, прошедших с последней границы символа
```

Два следствия. Соседние окна на расстоянии символа попадают в **один и тот же**
бин при любом выравнивании, поэтому преамбула видна как серия одинаковых бинов у
просто работающего коррелятора. И бин — не паразитная величина: он *и есть*
смещение timing в целых чипах, так что захват и грубый timing получаются одним
измерением.

Проверено на реальных отсчётах при SF7, `L = 8`; тождество выполняется с
точностью до округления:

| Смещение, отсчётов | CFO, бинов | Предсказание `d + cfo` | Измеренный бин |
|---:|---:|---:|---:|
| 137 | 0 | 110.875 | 111 |
| 137 | 3 | 113.875 | 114 |
| 999 | 0 | 3.125 | 3 |
| 1023 | 3 | 3.125 | 3 |

### Почему проверка sync — относительная

CFO тоже сдвигает тон после dechirp, поэтому бин равен `d + cfoBins`, а не `d`.
На преамбуле из одних upchirp эти два вклада неразличимы; разделяет их пара
downchirp SFD.

Поэтому подсистема проверяет sync word на `preambleBin + 8 x ниббл`, а не на
абсолютных позициях. CFO сдвигает преамбулу и символы sync на одинаковое число
бинов, так что относительная проверка к нему невосприимчива, а абсолютная — нет.
В таблице выше пара sync при смещении 137 переходит из `[119 127]` в `[122 2]`
при CFO в 3 бина, заворачиваясь через `N`, но остаётся ровно в 8 и 16 бинах от
преамбулы. Абсолютная проверка эти же самые отсчёты отвергает.

### Эквивалентность

Всюду целочисленная арифметика, поэтому DUT сравнивается с
`lora_phy.detect_preamble_run` на **точное равенство**, причём посимвольно, а не
по последовательностям: DUT скользит и переоценивает предикат на каждом символе
по сдвиговому регистру последних `PreambleSymbols + 2` бинов.

| SF | Символов преамбулы | N | Символов | Преамбул найдено | Расхождений решения | Расхождений бина |
|---:|---:|---:|---:|---:|---:|---:|
| 7 | 8 | 128 | 669 | 4 из 4 | 0 | 0 |
| 5 | 8 | 32 | 718 | 4 из 4 | 0 | 0 |
| 9 | 6 | 512 | 613 | 4 из 4 | 0 | 0 |

2000 символов, ноль расхождений по всем пяти выходам. Стимул — бины коррелятора
для пакетов, размещённых на смещениях, намеренно **не** кратных коэффициенту
передискретизации, с шумом до −10 дБ и с CFO, плюс псевдослучайные бины и
собранные вручную краевые случаи на бинах 0, 1, `N-1` и `N-8`.

Все **256 sync word** прогоняются на одной конфигурации, потому что именно там
DUT и эталон могли бы разойтись правдоподобнее всего: DUT приводит цель sync
битовой маской, а эталон — через `mod()`. Ноль расхождений. Маска точна здесь
только потому, что `N` — степень двойки, и это не микрооптимизация: `mod()` от
знакового значения — это деление с округлением Floor, которое HDL Coder вообще
отказывается генерировать.

### Попадание на границу: почему преамбула и sync — разные выходы

Разделение `preambleDetected` и `syncValid` не косметическое. На свободной сетке
окно может попасть на стык двух символов. Внутри преамбулы это ничего не стоит:
обе половины — один и тот же чирп, и бин не меняется. На стыке «преамбула — sync»
это уже стоит: окно несёт половину символа преамбулы и половину символа sync, в
спектре длины N стоят два сопоставимых пика, и трекер пика возвращает тот, что
больше.

Измерено на смещении около `M/2` — первый символ sync пропадает целиком:

```text
SF9 place3: 258 258 258 258 258 258 258  274 274     (ожидался sync 266, 274)
SF5 place3:  18  18  18  18  18  18  18   26  26 2   (ожидался sync 26,  2)
```

Развёртка по выравниваниям на протяжении целого символа — без шума, чтобы
выравнивание было единственной переменной, — даёт величину разрыва:

| Решение | Доля проходящих выравниваний |
|---|---:|
| `preambleDetected` | 100 % (32 из 32) |
| `syncValid` на свободной сетке | 97 % |

Значит, обнаружение преамбулы пригодно как есть, а проверка sync на этой сетке —
нет. Задуманная композиция: обнаружить преамбулу здесь, выровнять сетку окон по
`chipsToBoundary` — этот выход подсистема уже выдаёт, и развёртка подтверждает
его с точностью до одного чипа на каждом выравнивании, — и проверять sync на
выровненной сетке, где стыка уже нет.

Исходные таблицы:
[`simulink-m2-blind-detector.csv`](../data/simulink-m2-blind-detector.csv) и
[`simulink-m2-blind-detector-offsets.csv`](../data/simulink-m2-blind-detector-offsets.csv).

### Ложные срабатывания

Детектор, срабатывающий на шум, хуже, чем никакой. На 20 засеянных потоках из
одного шума — около 600 скользящих окон без сигнала — **ноль** обнаружений.
Требование восьми подряд бинов в пределах одного бина от первого плюс два бина
sync в цель — тяжёлый предикат для равномерного шума.

## Спайк M3: сгенерированный Verilog и первые числа по ресурсам

Генерация Verilog вынесена вперёд по порядку сознательно. Самым крупным
оставшимся пунктом M2 был acquisition, а вопрос «как строить слепой поиск
преамбулы» выглядел вопросом ресурсов: переиспользовать коррелятор с грубым
шагом или ставить отдельный дешёвый детектор — зависит от того, сколько
коррелятор уже стоит. Этого числа не существовало, поэтому оно измерено.

`run_hdl_generation` генерирует Verilog для DUT, идущих в железо, и читает
собственные отчёты HDL Coder.

| Цель | Файлов Verilog | Умножителей | Сумматоров | Регистров | 1-битных регистров | RAM | Мультиплексоров | Бит I/O | Добавленная latency |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| `fft-correlator-fixed` (SF7, L=8, 16 бит) | 77 | 34 | 437 | 1868 | 28169 | 64 | 989 | 185 | 34 |
| `blind-detector` | 2 | 0 | 44 | 11 | 168 | 0 | 72 | 73 | 0 |
| `acquisition` | 2 | 0 | 5 | 3 | 34 | 0 | 28 | 65 | 0 |
| `joint-timing-cfo` | 2 | 0 | 9 | 0 | 0 | 0 | 16 | 131 | 0 |

Все четыре генерируются с нулём ошибок HDL. Сгенерированный код зафиксирован в
[`fpga/generated/`](../../fpga/generated) и вручную не правится: изменения
поведения возвращаются в MATLAB и Simulink, после чего код генерируется заново.

Коррелятор стоит **34 умножителя и 64 RAM** в первой аппаратной точке — это
бюджет, вокруг которого укладывается всё остальное. Delay balancing HDL Coder
добавляет **34 такта** сверх модели, поэтому аппаратная latency символа при
SF7/L=8 равна 2414 + 34 = **2448 тактов отсчётов**, а не 2414 из таблицы
Simulink. Три целочисленные подсистемы вместе стоят 58 сумматоров, 14 регистров,
ни умножителей, ни RAM, ни добавленной задержки: это то, что даёт удержание
решений acquisition в целочисленной арифметике.

### Прогноз, в котором этот документ ошибся

Прежняя версия этого раздела утверждала о слепом поиске пакета:

> Значит, весь оставшийся бюджет достаётся отсутствующему слепому поиску
> пакета — и именно он его и израсходует.

Это оказалось неверно, и причину стоит зафиксировать. Оценка исходила из
скользящей корреляции по смещениям отсчётов, которая действительно доминировала
бы в проекте. Периодичность преамбулы убирает поиск целиком: обнаружение
переиспользует уже существующий выход коррелятора и добавляет предикат по
сдвиговому регистру из десяти бинов.

Не предсказано, а измерено: слепой детектор стоит **0 умножителей, 0 RAM, без
добавленной задержки и 168 бит регистров** — против 34 умножителей, 64 RAM и
28169 бит регистров у коррелятора. Это примерно 0.6 % его регистровых бит и
10 % его сумматоров.

Дорогим остаётся не обнаружение. Дорогим остаётся дробный ToA: ему нужен matched
filter на частоте отсчётов, а не чипов, и это единственная часть приёмника, где
цену скользящей корреляции обойти так и не удалось.

### Чем эти числа не являются

Это подсчёт операторов, выведенных HDL Coder из сгенерированного кода. Это
**не** результаты синтеза, и их нельзя читать как LUT/FF/DSP/BRAM. Один
умножитель здесь может лечь и в один DSP48, и в несколько, а счётчик RAM не
переводится напрямую в BRAM.

### Что заблокировано и чем именно

- **HDL cosimulation** не выполнялась. HDL-симулятор не установлен; лицензия
  HDL Verifier есть, но запускать нечего. Это пробел инструментов, а не проблема
  проекта.
- **Синтез, `Fmax`, timing и мощность** не измерены. Vivado на этом хосте нет.
  Присутствует только ISE 14.7, который не является целевым инструментом этого
  потока для Zynq. Ни частота, ни мощность нигде не заявляются.

## Ограничения и открытые пункты

Блокируют полную приёмку M2:

1. **Нет кадрирования пакета, и sync ещё не проверяется на выровненной сетке.**
   Слепое обнаружение преамбулы теперь есть, точно совпадает с MATLAB и
   восстанавливает смещение в целых чипах. Не хватает следующей ступени, которая
   это потребляет: выравнивания сетки окон по `chipsToBoundary`, проверки
   downchirp SFD и конечного автомата кадрирования, разводящего символы header и
   payload. Пока выравнивания нет, `syncValid` измеряется только на свободной
   сетке, где проходит 97 % выравниваний, а не 100 %.
2. **Дробный ToA не реализован.** Грубый счёт отсчётов и его флаг валидности
   реализованы и проверены, но `fractionalToaSamples` требует matched filter на
   частоте отсчётов, поэтому контракт метаданных выполнен наполовину. Это
   теперь самый дорогой непостроенный пункт приёмника.
3. **Нет cosimulation и синтеза.** Verilog сгенерирован для всех четырёх DUT,
   идущих в железо, с нулём ошибок HDL, счётчики операторов HDL Coder приведены
   выше. Чисел по `Fmax`, timing и мощности по-прежнему нет, и они не
   заявляются: на этом хосте нет ни HDL-симулятора, ни Vivado.
4. **Покрытие реальным сигналом — один корпус.** 130 пакетов SF5/SF6/SF7, все
   при `L = 8`, все — сильносигнальные OTA-записи. Это не sensitivity-тест и он
   не покрывает SF8–SF12, другие `L` и слабый сигнал.

Намеренно вне области M2, с задокументированным программным интерфейсом:
deinterleaving, FEC Хэмминга, dewhitening и CRC. Также вне области и остаются
в программной части: сопоставление TDoA, калибровка задержек и 2D
мультилатерация.
